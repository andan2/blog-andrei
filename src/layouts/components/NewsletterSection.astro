---
---
<section class="py-16 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="bg-gray-900 rounded-2xl overflow-hidden shadow-2xl">
        <div class="relative px-6 py-12 sm:px-12 lg:px-16">
            <div class="absolute inset-0 bg-pattern opacity-5"></div>
            <div class="relative z-10">
                <div class="max-w-2xl mx-auto text-center mb-10">
                    <h2 class="text-3xl font-extrabold text-gray-100 sm:text-4xl mb-4">
                        Únete a mi comunidad
                    </h2>
                    <p class="text-lg text-gray-300">
                        Recibe noticias exclusivas y ofertas especiales directamente en tu bandeja de entrada.
                    </p>
                </div>
                <form class="max-w-md mx-auto">
                    <div class="flex flex-col sm:flex-row gap-4">
                        <input
                            type="email"
                            placeholder="Tu correo electrónico"
                            class="w-full px-5 py-3 rounded-full text-gray-900 bg-gray-100 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-900 focus:ring-gray-500"
                            required
                        />
                        <button
                            type="submit"
                            class="w-full sm:w-auto px-8 py-3 bg-gray-700 text-gray-100 font-bold rounded-full hover:bg-gray-600 transition duration-300 transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-900 focus:ring-gray-500 shadow-lg"
                        >
                            Suscribirse
                        </button>
                    </div>
                </form>
                <p class="mt-8 text-sm text-center text-gray-400">
                    Valoro tu privacidad. Lee mi <a href="/politica-de-privacidad" class="font-medium underline hover:text-gray-200">Política de Privacidad</a>.
                </p>
            </div>
        </div>
    </div>
</section>
<style>
    .bg-pattern {
        background-image: radial-gradient(circle at 1px 1px, rgba(255, 255, 255, 0.1) 1px, transparent 0);
        background-size: 20px 20px;
    }
</style>